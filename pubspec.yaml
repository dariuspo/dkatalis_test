name: d_katalis
description: Darius Sandatinus _ One Day Recruitment Program
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.0.0
  bloc: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.0.5
  hive_generator: ^1.1.0
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

